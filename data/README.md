# data/ — place here (not committed)

This folder is intentionally empty in the repository. To run the analysis,
download the supplemental files and place them here:

- `metadata.csv`, `bile_acids.csv`, `ffq.csv`, `picrust_ko.csv`
- `len.rds` — de-identified phyloseq object, available from the corresponding
  author on request.
